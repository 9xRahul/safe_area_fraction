# Safe Area Fraction

A lightweight Flutter widget and context extension to apply layout padding
based on a fraction of the safe area and screen size.

## Example

```dart
SafeFraction(
  heightFraction: 0.1,
  widthFraction: 0.05,
  child: Container(color: Colors.blue),
)
